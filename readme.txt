to run the app
copy the extracted ctr folder and go to c://wamp/www and paste the folder
start wampServer and go to phpmyadmin 
create a new database called "ctra" select the database.
open the ctra folder and locate the file ctr.sql in the config folder
import the file to the ctra database.
the default username is "admin" and the password is "12345"
